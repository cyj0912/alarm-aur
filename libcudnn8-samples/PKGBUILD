pkgname=libcudnn8-samples
pkgver=8.2.1.32_1+cuda10.2
pkgrel=1
pkgdesc='cuDNN documents and samples'
arch=(aarch64)
url='https://repo.download.nvidia.com/jetson/'
license=('custom')
depends=('libcudnn8-dev|libcudnn8_dev_cross_aarch64' )
source=('https://repo.download.nvidia.com/jetson/common/pool/main/c/cudnn/libcudnn8-samples_8.2.1.32-1+cuda10.2_arm64.deb')
sha256sums=('52347d4fb10ef6db4897dd06407b41e46260bbd3c969d0886a739be7aa5f4e95')

prepare() {
  mkdir -p root
  tar xvf data.tar* -C root
}

package() {
  cp -a root/. $pkgdir/

  # On Arch, sbin is a symlink to bin
  if test -d $pkgdir/usr/sbin; then
    cp -a $pkgdir/usr/sbin/. $pkgdir/usr/bin/
    rm -rf $pkgdir/usr/sbin
  fi

  # Arch on ARM doesn't use multilib
  if test -d $pkgdir/lib/aarch64-linux-gnu; then
    cp -a $pkgdir/lib/aarch64-linux-gnu/. $pkgdir/lib/
    rm -rf $pkgdir/lib/aarch64-linux-gnu
  fi
  if test -d $pkgdir/usr/lib/aarch64-linux-gnu; then
    cp -a $pkgdir/usr/lib/aarch64-linux-gnu/. $pkgdir/usr/lib/
    rm -rf $pkgdir/usr/lib/aarch64-linux-gnu
  fi
}
