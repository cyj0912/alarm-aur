pkgname=python-vpi1
pkgver=1.1.15
pkgrel=1
pkgdesc='NVIDIA VPI python 2.7 bindings'
arch=(aarch64)
url='https://repo.download.nvidia.com/jetson/'
license=('custom')
depends=('libnvvpi1>=1.1' )
source=('https://repo.download.nvidia.com/jetson/common/pool/main/p/python-vpi1/python-vpi1_1.1.15_arm64.deb')
sha256sums=('3b0ce874666dbd0e2a7c858be215b1efd7fceb89259734bc3bf8cbb9edcf858c')

prepare() {
  mkdir -p root
  tar xvf data.tar* -C root
}

package() {
  cp -a root/. $pkgdir/
  # Arch on ARM doesn't use multilib
  if test -d $pkgdir/lib/aarch64-linux-gnu; then
    cp -a $pkgdir/lib/aarch64-linux-gnu/. $pkgdir/lib/
    rm -rf $pkgdir/lib/aarch64-linux-gnu
  fi
  if test -d $pkgdir/usr/lib/aarch64-linux-gnu; then
    cp -a $pkgdir/usr/lib/aarch64-linux-gnu/. $pkgdir/usr/lib/
    rm -rf $pkgdir/usr/lib/aarch64-linux-gnu
  fi
}
